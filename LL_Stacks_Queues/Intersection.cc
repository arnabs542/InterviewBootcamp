/*
Comments :
Given two singly linked lists, find their intersection.
The output should be the data in the first list.

Solution summary : find the length of both lists , let these be c1 and c2
Move pointer to the longer list forward by abs(c1-c2).
Then iterate pointers to both lists together.
*/

#include <iostream>

using namespace std;


int main(int argc, char** argv)
{
  return 0;
}

